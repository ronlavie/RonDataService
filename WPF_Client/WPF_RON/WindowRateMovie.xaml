<Window x:Class="WPF_RON.WindowRateMovie"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_RON"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="WindowRateMovie" Height="250" Width="250">
    <StackPanel>
        <TextBlock 
                   FontSize="20"
                   Text="{Binding Path=MovieName}"
                   TextAlignment="Center"
                   TextWrapping="WrapWithOverflow"
            Margin="0 15"/>
        <materialDesign:RatingBar Value="4"
                                  IsReadOnly="False"
                                  x:Name="RatingBar" Margin="0 15"/>
        <Button Content="Save rating"  Click="Save_Click"/>
    </StackPanel>
</Window>
